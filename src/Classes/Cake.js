export class Cake {

    constructor(name, image, options) { //since every cake has a name, image and options(small,medium,large with prices).
        this.name = name; 
        this.image = image;
        this.options = options;
    }

}