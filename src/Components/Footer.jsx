import React from "react";

export const Footer = ({}) => { //This is my fourth component named Footer, which is a functional component that doesn't take any props. It returns a simple footer element that contains a paragraph with the text "Have a great day my cupcakes!".

    return (
        <footer>
            <p>
                Have a great day my cupcakes!
            </p>
        </footer>
    );
};